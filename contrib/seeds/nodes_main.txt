# BurritoCoin mainnet fixed seed nodes.
#
# This list is consumed by contrib/seeds/generate-seeds.py to regenerate
# src/chainparamsseeds.h, which is compiled into the client. New nodes that
# can't yet reach a DNS seed fall back to dialling these IPs directly.
#
# Add additional reliable, long-running peers here as the network grows.
129.146.160.229:9227
